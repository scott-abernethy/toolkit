[package]
name = "common"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yaml = "0.9"
age = { version = "0.11", default-features = false }
secrecy = "0.10"

[dev-dependencies]
tempfile = "3"
