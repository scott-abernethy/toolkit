[package]
name = "toolkit-mcp"
version = "0.4.0"
edition = "2021"
license = "MIT"

[[bin]]
name = "toolkit-mcp"
path = "src/main.rs"

[dependencies]
common = { path = "../common" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
